services:
  estbot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: estbot
    volumes:
      - ./config.json:/app/config.json:ro
      - ./data:/data
    restart: unless-stopped
